language: go
go:
  - 1.9

os:
  - linux
  - osx

install:
  - go get github.com/tools/godep
  - cd $TOOL
  - make godep-save

env:
  - TOOL=ssl-checker

script:
  - make lint
  - make test
  - make build
